# AGENTS.md (public)

Start: say hi + 1 motivating line.
Work style: telegraph; noun-phrases ok; drop grammar; min tokens.

Repo: `tg-courier` (single-user Telegram → local CLI agent bridge).

Notes:
- Put personal/org instructions in `AGENTS.local.md` (gitignored).
- Keep diffs tight; keep files <~500 LOC; add regression tests when it fits.
